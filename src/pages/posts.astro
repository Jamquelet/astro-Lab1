---
const response = await fetch("https://jsonplaceholder.typicode.com/posts"); //asincrono await
console.log("response");
const posts = await response.json(); //convierteme el archivo a formato json
console.log(posts);
/* async function abc(){
    await sufuncion()
} */
---

<h1>Mis publicaciones</h1>

{
  posts.map((post) => (
    <div>
      <h5>{post.userId}</h5>
      <h3 class="title">{post.title}</h3>
      <p style="color: #333;">{post.body}</p>
    </div>
  ))
}

<style>
  .title {
    color: skyblue;
  }
</style>
